# 케이웨더 팀 준비물 — `agent.kweather.co.kr` 자체 서버 이관

이 플랫폼(웹 대시보드 + dApp + API)을 **케이웨더 자체 서버에서 `agent.kweather.co.kr` 도메인으로 독립 운영**하기
위해 케이웨더 팀이 준비/실행해야 할 항목입니다. 기술 상세 절차는 **[DEPLOY.md](DEPLOY.md)** 참고.

> 스마트컨트랙트(오라클·구독)는 BNB Smart Chain에 이미 배포되어 있어 **재배포 불필요**합니다.
> 이관 대상은 **웹+API 레이어뿐**이며 `node server.js` 단일 프로세스로 구동됩니다.

---

## 1. 준비물 체크리스트

### 🖥️ 인프라 (IT/인프라팀)
- [ ] **리눅스 서버 1대** — Ubuntu 22.04+ 권장, vCPU 2 / RAM 2GB / 디스크 10GB 면 충분
      (트래픽 적음 — 정적 + 경량 API). Node.js **20 LTS** 또는 Docker 설치 가능 환경.
- [ ] **공인 IP** 1개, 방화벽 **80/443 인바운드** 오픈
- [ ] 서버 → 외부 **아웃바운드 허용**: `gateway.kweather.co.kr:8443`(케이웨더 게이트웨이),
      `bsc-testnet-rpc.publicnode.com:443`(체인 RPC)

### 🌐 도메인/DNS (도메인 관리자)
- [ ] **`agent.kweather.co.kr` A 레코드 → 서버 공인 IP** 등록 (kweather.co.kr 존 관리 권한 필요)
- [ ] (TLS는 서버에서 Let's Encrypt 자동 발급 — 별도 인증서 구매 불필요)

### 🔑 키/계정 (서비스 담당)
- [ ] **케이웨더 게이트웨이 API 키** — **세계날씨(kw-world-r1/3d1)** + **국내(kw-odam1, kw-dust-r1)** 권한 포함
- [ ] **릴레이어 지갑 개인키** — 온체인 날씨 발행용 (BSC 테스트넷 tBNB 보유 필요)
- [ ] **에이전트 지갑 개인키** — 자율 에이전트 데모용 (KWT + tBNB 보유)
      ※ 현재 키/지갑은 wellbianlabs가 보유 중 → 운영 주체에 맞게 인계 또는 신규 발급 결정 필요

### ⛓️ 체인/정산 (블록체인 담당) — 현재 **테스트넷**
- [ ] 릴레이어 지갑에 **tBNB(가스)** 펀딩 (BSC 테스트넷 faucet)
- [ ] (선택) 메인넷 전환 시: 실 BNB 가스 + `X402_ENABLE_MAINNET=true` + 메인넷 RPC

---

## 2. 실행 절차 (요약)

```bash
git clone https://github.com/wellbianlabs/kweather-aivm-oracle.git
cd kweather-aivm-oracle
cp .env.example .env          # 키/지갑/주소 입력 (DEPLOY.md 표 참고)
npm ci --omit=dev
node server.js                # 또는 Docker / systemd (DEPLOY.md 3~4장)
```
이후 **nginx 리버스프록시 + certbot TLS**로 `agent.kweather.co.kr` 연결, **크론**으로 시간당 갱신
(DEPLOY.md 5~6장). 확인: `https://agent.kweather.co.kr/api/quote`.

---

## 3. 책임 분담 요약

| 항목 | 담당 | 비고 |
|---|---|---|
| 서버/네트워크/방화벽 | 케이웨더 인프라팀 | 사양·아웃바운드 위 참고 |
| `agent.kweather.co.kr` DNS | 케이웨더 도메인 관리자 | A 레코드 → 서버 IP |
| TLS 인증서 | (서버에서 자동) | Let's Encrypt / certbot |
| 케이웨더 API 키 | 케이웨더 서비스 담당 | 세계 + 국내 권한 |
| 릴레이어/에이전트 지갑·펀딩 | 운영 주체 결정 | 현재 wellbianlabs 보유 |
| 앱 구동/배포 스크립트 | 제공 완료 (이 저장소) | server.js / Docker / DEPLOY.md |
| 컨트랙트 | 배포 완료 (재배포 불필요) | BSC 테스트넷 |

---

## 4. 우리 서비스가 실제로 제공 가능한 날씨 범위

**모두 케이웨더 실측 데이터**이며, 위 라이브 테스트로 실데이터 응답을 확인했습니다.

| 구분 | 제공 지역 수 | 데이터 소스 | 제공 항목 |
|---|---|---|---|
| **세계** | **4,255개 도시 / 167개국** | K-Weather 세계날씨 `kw-world-r1`(실황) + `kw-world-3d1`(시간예보) | 기온·체감온도·습도·강수·풍속·풍향·기압·가시거리·적설·불쾌지수 |
| **국내** | **3,561개 법정동(읍면동) / 17개 시도** | K-Weather 국내 `kw-odam1`(관측) + `kw-dust-r1`(미세먼지) | 기온·체감·습도·강수·풍속·풍향·**PM10·PM2.5**·불쾌지수 |
| **합계** | **7,816개 지역** | — | — |

- **세계 국가 분포(상위)**: 중국 421 · 인도 369 · 브라질 294 · 미국 293 · 일본 263 · 러시아 170 · 멕시코 116 · 필리핀 114 …
- **온체인 등록 현황**: 국내 **3,561개 전수 등록 완료**, 세계 **418개 발행** + 나머지는 조회 시 즉시 발행(on-demand relay).
- **확장 여지**: 케이웨더 세계날씨 원천에는 약 5,479개 도시가 있어, 매핑 확대 시 세계 커버리지를 더 늘릴 수 있습니다.
  국내는 읍면동(3,561) 외에 시군구(약 252)·시도(17) 레벨도 케이웨더가 제공하므로 필요 시 추가 가능합니다.
- **참고**: 세계날씨에는 PM/일사/자외선이 없어 해당 의사결정 상품은 **국내(PM 포함)** 에만 적용됩니다.
